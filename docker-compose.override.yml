# Local test override — production'da kullanılmaz
services:
  traefik:
    profiles: ["production-only"]

  web:
    ports:
      - "3000:3000"
    environment:
      BETTER_AUTH_URL: http://localhost:3000
      CORS_ORIGIN: http://localhost:3000
    labels:
      - "traefik.enable=false"

  canton-sandbox:
    ports:
      - "6865:6865"
      - "7575:7575"
